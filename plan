src/
├── main.rs                     # парсинг аргументов, загрузка/сохранение
│
├── config.rs                   # конфигурация сервера и БД (не состояние!)
│   ServerConfig                # port, main_directory, возможно thread counts, log level и т.д.
│   TableConfig                 # only_ram, only_rom, rows_limit, emergency_expand, save_path
│
└── auth.rs                     # всё, что связано с ролями и пользователями
    struct Role
    struct User
    fn check_permission(user: &User, action: Action) → Result<(), DbError>
    enum Action { EditRoles, EditTables, ReadTable, AppendToTable, ... }